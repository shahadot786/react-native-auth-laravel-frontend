import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const GreetingsImagePicker = () => {
  return (
    <View>
      <Text>GreetingsImagePicker</Text>
    </View>
  );
};

export default GreetingsImagePicker;

const styles = StyleSheet.create({});
